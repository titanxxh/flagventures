# Flagventures

## 打开应用

- **在线使用**：[打开Flagventures](https://titanxxh.github.io/flagventures/)。
- **本地使用**：双击 `dist/Flagventures.html`，在 Chrome 中打开。教学内容随网页保存，不需要启动服务、安装软件或联网。

两个版本均保留全部 64 个教学条目。默认应用不包含原页图片，也不需要 PDF 或原页图片才能运行。

如果文本编辑器打开了 HTML，在 Finder 里右键文件 → 打开方式 → Google Chrome。

## 一次 5–10 分钟

1. **选内容**：左侧先展开分类，再选教学条目；「进攻阵型与战术」内还可展开阵型组，选择阵型站位或战术 1、2、3。也可以搜索分类名称、中文、英文跑法或页码。
2. **认站位**：先找自己的字母，看看关键队友站在哪里。
3. **看跑法和配合**：鼠标停在球员或字母按钮上，查看中英文术语、队友配合及启动时机。点击可保留；用 Tab 键也能查看。30 套进攻战术还提供全队目标和讲解提问。
4. **停住讲解**：点任意关键帧，全部球员一起定位并保持暂停。点「继续播放」从那里接着走。拖动进度也会暂停。
5. **换位回看**：回到站位，换一个角色，让孩子用手指讲一遍自己和队友的路线。

基础路线、进攻阵型与战术、跑球战术、防守分工都可点击标题展开或收起；进攻分类内保留 10 个阵型组。首次打开内置手册时，只展开当前条目所在的「基础路线」。

搜索分类名称会显示该分类下的全部条目；其他搜索会保留匹配条目所属的分类和组标题，并自动展开结果。用「上一条／下一条」切换时，会自动展开当前条目所在的分类及阵型组。

默认以 **2×** 播放。播放按钮下方可选择 **0.5×、1×、2×、3×**；暂停时调速会保持暂停和当前位置，切换教学条目也会保留所选速度。

连续播放经过关键帧不会自动暂停。路线存在多个分支或条件时，先选好要演示的情形；更改选择会回到起点并暂停。例如“其他队友没有空位时中锋再接应”，需要先选择这次是否演示中锋接应；应用不会自动判断防守。

## 理解资料与动画

- 画面中的坐标与秒数用于教学，不是实测码数或比赛节奏。
- 原 PDF 没有给出统一的跨球员起跑先后；部分进攻战术根据补充的官方讲解安排先后、等待或条件释放。具体秒数仍是教学设置，详见「跑动先后与资料说明」。
- 没有给出后续动作的球员只显示已知站位，不能理解为比赛中必须站住。
- 防守区域是职责范围，虚线是原图指示，不是沿区域边界奔跑。
- 术语依据分为来源明确命名、路线形态对照、按图描述、作者编写和资料未说明。形态相似不等于来源已为这条路线命名；图文存在差异时保留说明。
- 默认应用保留来源名称、页码及官方网页/视频链接，不附带原页图片。中文来自非官方学习译本及教学编写。讲解离线可用，主动点击资料或视频链接后才联网。
- 自己导入的战术包可以带有可选参考图片；提供图片的条目可查看对照。应用不会读取原始 PDF。

## 新增或修改自己的战术

1. 点右上角「我的战术文件」→「下载新增模板」。也可下载当前条目的 YAML，再复制修改。
2. 用支持纯文本的编辑器打开 `.yaml`。macOS 文本编辑器请选择纯文本格式，保留 `.yaml` 扩展名和 UTF-8 编码。
3. 修改 `id`、名称、说明、球员坐标、路线和关键帧。**新增战术要改成新的 `id`**；沿用旧 `id` 会更新旧条目。
4. 点「导入 YAML / 战术包」选择文件，可以一次选择多个 YAML。
5. 查看新增／更新清单，再点「确认应用」。有错误会指出文件与字段，当前手册不变。
6. 点「保存完整战术包」，把 `.flagbook.json` 文件保存到自己的文件夹。

下次打开在线应用或本地网页，先显示内置手册；导入自己保存的战术包即可继续使用新增内容。网页不会自动修改原 YAML 文件，也不自动保存到浏览器。

### 两种导入的区别

| 文件 | 应用后的结果 |
| --- | --- |
| 一个或多个 `.yaml` / `.yml` | 新编号追加到「我的战术」；已有编号整条更新、保持目录位置 |
| 一个 `.flagbook.json` | 用该包的完整条目、目录和可选图片替换本次活动库 |

更新为整条替换：新文件没写的可选字段，不从旧条目继承。整包和 YAML 不能混在同一次导入中。需要保留当前修改时，先保存完整战术包。

格式详解、全部字段和示例见 [战术文本格式](content-format/README.md)。普通新增不需要 `source` 或 PDF；图片和目录分组都是可选的。直接导入的新 YAML 会列在「我的战术」中；需要分组时，在目录或完整战术包中配置。

## 文件在哪里

- [在线应用](https://titanxxh.github.io/flagventures/)
- [本地应用成品](dist/Flagventures.html)
- 完整战术包：在应用的「我的战术文件」中选择「保存完整战术包」。
- [每条内容的 YAML](content/lessons/)
- [默认目录](content/catalog.yaml)
- [新增模板](content-format/templates/new-play.yaml)
- [数据格式说明](content-format/README.md)

学习进度、本次关注的角色和分支选择不写入战术包。战术包保存的是教学内容。

## 切换语言

右上角选择 **中文 / English**。界面和 64 个内置教学条目都会切换，包括跑法说明、队友配合、关键帧及资料说明。切换时保留当前战术、播放进度、速度、关注球员和条件选择。

浏览器允许本地存储时会记住语言偏好；教学内容仍需自行导出保存。两种语言均可离线使用。自编战术可在 YAML 中填写 `translations.en`，缺少英文时保留原文，详见文本格式说明。
