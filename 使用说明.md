# 腰旗小教练

## 打开应用

- **在线使用**：[打开腰旗小教练](https://titanxxh.github.io/flagventures/)。
- **本地使用**：双击 `dist/腰旗小教练.html`，在 Chrome 中打开。教学内容随网页保存，不需要启动服务、安装软件或联网。

两个版本均保留全部 64 个教学条目。默认应用不包含原页图片，也不需要 PDF 或原页图片才能运行。

如果文本编辑器打开了 HTML，在 Finder 里右键文件 → 打开方式 → Google Chrome。

## 一次 5–10 分钟

1. **选内容**：左侧按原书顺序浏览，也可以搜索中文、英文跑法或页码。
2. **认站位**：先找自己的字母，看看关键队友站在哪里。
3. **看跑法**：鼠标停在球员或字母按钮上，查看中英文术语、说明和依据。点击可保留；用 Tab 键也能查看。
4. **停住讲解**：点任意关键帧，全部球员一起定位并保持暂停。点「继续播放」从那里接着走。拖动进度也会暂停。
5. **换位回看**：回到站位，换一个角色，让孩子用手指讲一遍自己和队友的路线。

「慢一点」以半速演示。连续播放经过关键帧不会自动暂停。路线存在多个分支时，先选好要演示的路线；更改选择会回到起点。

## 理解资料与动画

- 画面中的坐标与秒数用于教学，不是实测码数或比赛节奏。
- 原书没有给出统一的跨球员起跑先后；每条内容会在「跑动先后与资料说明」解释使用的教学时序。
- 没有给出后续动作的球员只显示已知站位，不能理解为比赛中必须站住。
- 防守区域是职责范围，虚线是原图指示，不是沿区域边界奔跑。
- 术语依据分为原书标注、路线形态对照、按图描述、作者编写和资料未说明。形态相似不等于原书为这条路线命名。
- 默认应用保留来源名称、页码及说明，不附带原页图片。中文来自非官方学习译本及教学编写。
- 自己导入的战术包可以带有可选参考图片；提供图片的条目可查看对照。应用不会读取原始 PDF。

## 新增或修改自己的战术

1. 点右上角「我的战术文件」→「下载新增模板」。也可下载当前条目的 YAML，再复制修改。
2. 用支持纯文本的编辑器打开 `.yaml`。macOS 文本编辑器请选择纯文本格式，保留 `.yaml` 扩展名和 UTF-8 编码。
3. 修改 `id`、名称、说明、球员坐标、路线和关键帧。**新增战术要改成新的 `id`**；沿用旧 `id` 会更新旧条目。
4. 点「导入 YAML / 战术包」选择文件，可以一次选择多个 YAML。
5. 查看新增／更新清单，再点「确认应用」。有错误会指出文件与字段，当前手册不变。
6. 点「保存完整战术包」，把 `.flagbook.json` 文件保存到自己的文件夹。

下次打开在线应用或本地网页，先显示内置手册；导入自己保存的战术包即可继续使用新增内容。网页不会自动修改原 YAML 文件，也不自动保存到浏览器。

### 两种导入的区别

| 文件 | 应用后的结果 |
| --- | --- |
| 一个或多个 `.yaml` / `.yml` | 新编号追加到「我的战术」；已有编号整条更新、保持目录位置 |
| 一个 `.flagbook.json` | 用该包的完整条目、目录和可选图片替换本次活动库 |

更新为整条替换：新文件没写的可选字段，不从旧条目继承。整包和 YAML 不能混在同一次导入中。需要保留当前修改时，先保存完整战术包。

格式详解、全部字段和示例见 [战术文本格式](content-format/README.md)。普通新增不需要 `source` 或 PDF；图片也是可选的。

## 文件在哪里

- [在线应用](https://titanxxh.github.io/flagventures/)
- [本地应用成品](dist/腰旗小教练.html)
- 完整战术包：在应用的「我的战术文件」中选择「保存完整战术包」。
- [每条内容的 YAML](content/lessons/)
- [默认目录](content/catalog.yaml)
- [新增模板](content-format/templates/new-play.yaml)
- [数据格式说明](content-format/README.md)

学习进度、本次关注的角色和分支选择不写入战术包。战术包保存的是教学内容。
